mutation PostponeNewsList($input: PostponeNewsListInput!) {
  postponeNewsList(input: $input) {
    ...NewsFragment
    likes {
      ...LikeFragment
    }
    user {
      ...UserFragment
    }
  }
}
